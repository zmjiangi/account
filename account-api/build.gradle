archivesBaseName = 'account-api'

task sourcesJar(type: Jar) {
    classifier = 'sources'
    from sourceSets.main.allSource
}

artifacts {
    archives jar
    archives sourcesJar
}

dependencies {
    compileOnly 'org.springframework.data:spring-data-mongodb:1.10.10.RELEASE'
}
