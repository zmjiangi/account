spring:
  cloud:
    config:
      fail-fast: true
      server:
        bootstrap: true
        prefix: /status/config
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8088/eureka/
