spring:
  application:
    name: account
  cloud:
    config:
      fail-fast: true
      server:
        bootstrap: true
        prefix: /status/configserver
  profiles:
    active: native
  jmx:
    default-domain: server