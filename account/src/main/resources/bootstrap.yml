spring:
  application:
    name: account
  cloud:
    config:
      fail-fast: true
      server:
        native:
          searchLocations: file:///etc/account/config
        bootstrap: true
        prefix: /status/configserver
  profiles:
    active: native
  jmx:
    default-domain: server